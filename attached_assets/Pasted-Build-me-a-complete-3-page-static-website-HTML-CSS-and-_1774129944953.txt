Build me a complete 3-page static website — HTML, CSS, and vanilla JavaScript in a single index.html file with internal <style> and <script> tags. No frameworks, no build tools, no external dependencies except Google Fonts.
The three pages are: a landing page (#home), a form page (#input), and a thank you page (#thankyou). Use hash-based navigation to show and hide pages — only one page visible at a time, no full page reloads.
The design specification, all copy, colour tokens, typography choices, photo assignments, and form logic are in the markdown file below. Follow it precisely. Do not invent copy, substitute fonts, or change colour values. Where the spec says something like [heading — Playfair Display 32px italic, Store Green], that is a styling instruction for the text that follows it — not literal text to render on screen.
A few things the spec doesn't cover that you need to handle:
Images: All photos referenced in the spec are not yet hosted. Use the exact filenames from the spec as src values (e.g. src="A2278532_8.jpg"). They will be dropped into the same directory as the HTML file. Do not use placeholder services like picsum or unsplash — leave the src paths as-is so real photos can be swapped in.
Video: Embed the YouTube documentary using youtube-nocookie.com in a standard iframe. 16:9 ratio via aspect-ratio: 16/9. No autoplay. The URL is in the spec.
Form submission: Wire the form to Formspree using the endpoint https://formspree.io/f/REPLACE_WITH_ID. On successful fetch response, navigate to the thank-you page. On failure, show the inline error message specified in the spec above the submit button. Use fetch() with method: POST and Accept: application/json.
Grain texture: Implement as an SVG data URI applied via a fixed ::before pseudo-element on the body — no external file.
Animations: The hero image slow-zoom and the staggered text fade-up on page load should use CSS @keyframes. Re-trigger them each time the home page becomes visible. Keep all other animations CSS-only.
Radio buttons: Style them completely custom — no default browser appearance. The full row of each option should be clickable. Selected state is Store Green background with Warm White text. Use a real <input type="radio"> inside each label for accessibility.
Mobile: Design and code mobile-first. The layout should be fully usable and visually correct at 375px width. Navigation collapses gracefully. The form is full width with 16px horizontal padding.
Output the complete file in one code block. Do not truncate. Do not add explanatory text outside the code block.